<?xml version="1.0" encoding="UTF-8"?>
<!--
  Second custom dialog: where the exported data comes from.

  Every field here is optional. NetBox Local works perfectly well as a
  standalone local NetBox that is filled by hand, in which case the whole API
  side simply stays switched off. The fields only matter when there is a
  self-hosted NetBox to mirror.
-->
<Wix xmlns="http://schemas.microsoft.com/wix/2006/wi">
  <Fragment>

    <Property Id="SYNCROOT" Value="C:\Sync-Daten\NetBoxLocal" />
    <Property Id="NBSERVER" Secure="yes" />
    <Property Id="NBAPIKEY" Hidden="yes" />
    <Property Id="SCRIPTPATH" Value="C:\Sync-Skripte\NetBoxLocal" />
    <Property Id="AUTOIMPORT" Value="1" />

    <UI>
      <Dialog Id="SyncSourceDlg" Width="370" Height="270" Title="[ProductName] Setup">

        <Control Id="BannerBitmap" Type="Bitmap" X="0" Y="0" Width="370" Height="44"
                 TabSkip="no" Text="!(loc.InstallDirDlgBannerBitmap)" />
        <Control Id="BannerLine" Type="Line" X="0" Y="44" Width="370" Height="0" />
        <Control Id="BottomLine" Type="Line" X="0" Y="234" Width="370" Height="0" />

        <Control Id="Title" Type="Text" X="15" Y="6" Width="250" Height="15" Transparent="yes"
                 NoPrefix="yes" Text="{\WixUI_Font_Title}Data source (optional)" />
        <Control Id="Description" Type="Text" X="25" Y="23" Width="330" Height="15" Transparent="yes"
                 NoPrefix="yes" Text="Only needed to mirror an existing NetBox. Leave empty to use it standalone." />

        <Control Id="Intro" Type="Text" X="20" Y="52" Width="335" Height="24"
                 Text="Skip this page if you have no self-hosted NetBox. These settings can also be added later in config\NetBoxLocal.json." />

        <Control Id="SyncLabel" Type="Text" X="20" Y="82" Width="200" Height="10"
                 Text="&amp;Exported data folder:" />
        <Control Id="SyncEdit" Type="PathEdit" X="20" Y="94" Width="270" Height="17" Property="SYNCROOT" />
        <Control Id="SyncBrowse" Type="PushButton" X="295" Y="94" Width="56" Height="17"
                 Text="B&amp;rowse...">
          <Publish Property="_BrowseProperty" Value="SYNCROOT" Order="1">1</Publish>
          <Publish Event="SpawnDialog" Value="BrowseDlg" Order="2">1</Publish>
        </Control>
        <Control Id="SyncHint" Type="Text" X="20" Y="113" Width="335" Height="10"
                 Text="Where the export writes Monday.zip ... Sunday.zip." />

        <Control Id="ServerLabel" Type="Text" X="20" Y="131" Width="200" Height="10"
                 Text="NetBox &amp;server address:" />
        <Control Id="ServerEdit" Type="Edit" X="20" Y="143" Width="331" Height="17" Property="NBSERVER" />
        <Control Id="ServerHint" Type="Text" X="20" Y="162" Width="335" Height="10"
                 Text="For example https://netbox.example.com/ - leave empty to skip the export." />

        <Control Id="KeyLabel" Type="Text" X="20" Y="180" Width="200" Height="10"
                 Text="API &amp;token (read permissions only):" />
        <Control Id="KeyEdit" Type="Edit" X="20" Y="192" Width="331" Height="17"
                 Property="NBAPIKEY" Password="yes" />
        <Control Id="KeyHint" Type="Text" X="20" Y="211" Width="335" Height="18"
                 Text="Must not have write permissions. A lost notebook would otherwise grant full access to the production NetBox." />

        <Control Id="Back" Type="PushButton" X="180" Y="243" Width="56" Height="17"
                 Text="!(loc.WixUIBack)" />
        <Control Id="Next" Type="PushButton" X="236" Y="243" Width="56" Height="17"
                 Default="yes" Text="!(loc.WixUINext)" />
        <Control Id="Cancel" Type="PushButton" X="304" Y="243" Width="56" Height="17"
                 Cancel="yes" Text="!(loc.WixUICancel)">
          <Publish Event="SpawnDialog" Value="CancelDlg">1</Publish>
        </Control>

      </Dialog>
    </UI>

  </Fragment>
</Wix>
